const CONFIG = {
  // Frontend (Vite dev server) — used only for opening the app
  APP_URL: 'http://localhost:5173',
  // Backend (FastAPI) — all API calls go here
  API_URL: 'http://localhost:8000',
}
